name: Test Spark Image

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Pull Spark Image
        run: |
          docker pull apache/spark:3.5.4
          docker images
