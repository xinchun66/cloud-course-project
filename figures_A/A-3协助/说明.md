# A-3 协助运行截图说明

本目录保存成员 A 在 CCE 集群中协助成员 B 运行 SparkApplication 的过程截图。A-3 使用成员 B 提供的镜像：

`swr.cn-north-4.myhuaweicloud.com/xinchunli/pyspark:3.5.4-a3`

运行入口为：

`local:///opt/spark/work/analysis.py`

Spark 版本为：

`3.5.4`

截图内容如下：

- `01_executor1_SparkApplicationYAML参数.png`：`executor.instances=1` 的关键 YAML 参数。
- `02_executor1_DriverCompleted状态.png`：`executor.instances=1` 运行完成状态。
- `03_executor1_Driver日志性能输出.png`：`executor.instances=1` 的 Driver 日志关键输出。
- `04_executor2_SparkApplicationYAML参数.png`：`executor.instances=2` 的关键 YAML 参数。
- `05_executor2_DriverCompleted状态.png`：`executor.instances=2` 运行完成状态。
- `06_executor2_Driver日志性能输出.png`：`executor.instances=2` 的 Driver 日志关键输出。

当前日志截图可证明两次运行均使用指定镜像和 `analysis.py`，并输出了 Spark executor 数量与 PySpark 查询耗时。若后续成员 B 调整脚本以额外输出 Pandas 查询耗时、性能 CSV、Amdahl 汇总 CSV 和图表保存路径，可替换对应日志截图。
